nowhitespaces=content
   several white spaces = con tent    
multiline keyword = this is a \
     long line which was split into mult\
     iple lines

after linebreak = ok

missing value
missing multiline \
   value
missing keyword definition = ok

duplicate keyword = old
duplicate keyword = new

userPreparser active = moo substituee
